# How to build the project:
1. Install the latest version of GNU C++ Compiler from https://gcc.gnu.org/
2. Install CMake.
3. Run the following commands:
   mkdir build
   cd build
   cmake ..
   make
